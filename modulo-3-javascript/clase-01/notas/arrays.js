// Los arrays son estructuras dentro de los lenguajes de programación que nos permiten almacenar diferentes tipos de datos dentro del mismo.

// Sintáxis
    let nombre_array = [];
/* 
    * Los elementos que se almacenan en el array, se los identifica mediante un número llamado indice o posición. El primer elemento tiene un indice igual a cero:

        Índices     =>          0             1             2      
        Elementos   =>    ['elemento_1', 'elemento_2', 'elemento_3', ...];
*/

// Ejemplo
    let ejemplo_array = ["hola", 2, true, null, undefined, function(){}];

// Accediendo a un elemento del array
    console.log(ejemplo_array[0]); // "hola"
    console.log(ejemplo_array[1]); // "2"
