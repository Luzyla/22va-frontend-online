 // Sintaxis
if (condicion_1) {
  // Código a ejecutar si la condición 1 se cumple
} else if (condicion_2) {
  // Código a ejecutar si la condición 1 no se cumple y la condición 2 si se cumple
} else {
  // Código a ejecutar si las dos condiciones anteriores no se cumplieron
}
