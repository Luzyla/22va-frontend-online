 // Sintaxis
if (condicion_1) {
  // Código a ejecutar si la condición 1 se cumple
  if (condicion_anidada) {
    // Código a ejecutar si la condicion_anidada se cumple
  }
} else {
  // Código a ejecutar si la condición no se cumple
}
